import { GameControllerDirective } from './game-controller.directive';

describe('GameControllerDirective', () => {
  it('should create an instance', () => {
    const directive = new GameControllerDirective();
    expect(directive).toBeTruthy();
  });
});
